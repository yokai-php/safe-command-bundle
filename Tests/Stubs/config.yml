framework:
    test: true
    secret: test
    router:
        resource: "%kernel.root_dir%/routing.yml"

yokai_safe_command:
    environments:
        enabled: true
        environments:
            - test
